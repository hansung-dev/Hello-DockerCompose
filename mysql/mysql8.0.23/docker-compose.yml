version: '3'

services:
  mysql01:
    container_name: mysql01
    hostname: mysql01
    image: mysql:8.0.23
    command: mysqld --default-authentication-plugin=mysql_native_password --character-set-server=utf8mb4 --collation-server=utf8mb4_unicode_ci
    environment:
      MYSQL_ROOT_PASSWORD: root
    ports:
      - '3306:3306'
    volumes:
      - "${PWD}/mysql01/data:/var/lib/mysql"
      - "${PWD}/mysql01/log:/var/log/mysql"
      - "${PWD}/mysql01/conf/my.cnf:/etc/mysql/conf.d/my.cnf"
    networks:
      - mybridge
networks:
  mybridge:
    external: true
